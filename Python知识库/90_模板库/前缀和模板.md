---
created: 2026-05-31
type: note
tags: [模板库, 前缀和]
---

# 前缀和模板

```python
n = int(input())
nums = list(map(int, input().split()))

prefix = [0] * (n + 1)
for i in range(n):
    prefix[i + 1] = prefix[i] + nums[i]

# 查询 [l, r]，0 下标，包含 r
ans = prefix[r + 1] - prefix[l]
```

相关：[[前缀和]]
