---
created: "2026-05-18"
updated: "2026-05-18"
area: "语法"
kind: "教材"
level: "基础"
status: "常用"
priority: "P1"
source:
  - "计算机知识"
  - "原始笔记"
  - "全量代码遍历"
tags:
  - "python/语法"
  - "状态/常用"
aliases:
  - "函数"
  - "solve"
  - "拆分"
---

# 函数

> 函数不是为了显得高级，是为了让脑子一次只装一件事。

## 最小刷题函数

```python
def solve():
    n = int(input())
    nums = list(map(int, input().split()))
    # 核心逻辑
    print(ans)

solve()
```

## 什么时候值得写函数

- 质数判断。
- 最大公约数如果不用 `math.gcd()`。
- 一段逻辑要重复用。
- 主程序开始超过 30 行，看着乱。

## 例子：质数判断

```python
def is_prime(n):
    if n < 2:
return False
    i = 2
    while i * i <= n:
if n % i == 0:
    return False
i += 1
    return True
```

## 给你的提醒

你在 `stats.py` 里已经能写结构清晰的函数，所以不是不会。刷题时可以先不追求复杂封装，但至少试着让主逻辑有边界。
