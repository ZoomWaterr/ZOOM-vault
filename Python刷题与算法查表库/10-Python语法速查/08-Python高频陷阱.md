# Python 高频陷阱

## 语法陷阱

- `input()` 是字符串。
- `/` 返回 float。
- `sort()` 改原列表，`sorted()` 返回新列表。
- 字符串方法不改原字符串。
- `join()` 只能拼字符串。
- `{}` 是空字典，`set()` 才是空集合。

## 刷题陷阱

- 二维列表不要复制同一行。
- 下标 `i + 1` 要防越界。
- 嵌套循环别让 `i/j` 打架。
- 样例过了不代表边界过了。
